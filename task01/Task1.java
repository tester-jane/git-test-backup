package task01;

public class Task1 {

	public static void main(String[] args) {
		double x = 15.5;
		double y = 0.4;
		double sum = x + y;
		double diff = x - y;
		double mult = x * y;
		double div = x / y;
		System.out.print("sum = " + sum + "; difference = " + diff + "; product = " + mult + "; quotient = " + div);
		
		
	}

}
